Create a minimal but strongly validated spec-first starter for PM/design agents. The source is a sparse directly authored spec (`spec/spec.yaml`) that compiles into layer-specific agent prompts, `spec/generated/compiled/spec.yaml`, OpenAPI, AsyncAPI, CWL, routing, state-machine surface specs, one generated Gherkin corpus for both pytest-bdd harnesses, and full-spec visual audit renders. The PM/design spec must not contain test-harness routing, dev-environment details, SQL, database dialects, migrations, or ORM/storage implementation details.

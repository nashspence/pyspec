asyncapi: 3.1.0
channels:
  event_event_project_approved:
    address: event.project.approved
    messages:
      message_event_project_approved:
        $ref: '#/components/messages/message_event_project_approved'
    x-event: event.project.approved
  event_event_project_archived:
    address: event.project.archived
    messages:
      message_event_project_archived:
        $ref: '#/components/messages/message_event_project_archived'
    x-event: event.project.archived
  event_event_project_created:
    address: event.project.created
    messages:
      message_event_project_created:
        $ref: '#/components/messages/message_event_project_created'
    x-event: event.project.created
  event_event_project_submitted:
    address: event.project.submitted
    messages:
      message_event_project_submitted:
        $ref: '#/components/messages/message_event_project_submitted'
    x-event: event.project.submitted
components:
  messages:
    message_event_project_approved:
      name: event.project.approved
      payload:
        $ref: '#/components/schemas/data_contract.project.approved'
      title: Event Project Approved
      x-emitted-by:
      - application_action.project.approve
    message_event_project_archived:
      name: event.project.archived
      payload:
        $ref: '#/components/schemas/Project'
      title: Event Project Archived
      x-emitted-by:
      - application_action.project.archive
    message_event_project_created:
      name: event.project.created
      payload:
        $ref: '#/components/schemas/Project'
      title: Event Project Created
      x-emitted-by:
      - application_action.project.create
    message_event_project_submitted:
      name: event.project.submitted
      payload:
        $ref: '#/components/schemas/Project'
      title: Event Project Submitted
      x-emitted-by:
      - application_action.project.submit
  schemas:
    Problem:
      additionalProperties: false
      properties:
        code:
          type: string
        message:
          type: string
      required:
      - code
      - message
      type: object
    Project:
      additionalProperties: false
      properties:
        assignee:
          type: string
        created_at:
          format: date-time
          type: string
        customer:
          type: string
        id:
          type: string
        priority:
          type: string
        status:
          enum:
          - draft
          - submitted
          - approved
          - archived
          type: string
        summary:
          type: string
        title:
          type: string
        updated_at:
          format: date-time
          type: string
        workspace_id:
          type: string
      required:
      - assignee
      - created_at
      - customer
      - id
      - priority
      - status
      - summary
      - title
      - updated_at
      - workspace_id
      type: object
    data_contract.project.approved:
      additionalProperties: false
      properties:
        approved_by:
          type: string
        project_id:
          type: string
      required:
      - approved_by
      - project_id
      type: object
    data_contract.project.notice_result:
      additionalProperties: false
      properties:
        notice_id:
          type: string
        queued_at:
          format: date-time
          type: string
      required:
      - notice_id
      - queued_at
      type: object
info:
  title: project_dispatch_board
  version: 0.0.0-contract
operations:
  receive_workflow_project_approval_notice:
    action: receive
    channel:
      $ref: '#/channels/event_event_project_approved'
    messages:
    - $ref: '#/components/messages/message_event_project_approved'
    x-contract-ref: workflow.project.approval_notice
    x-dispositions:
      entry_point.worker.project.approval_notice:
        accepted:
          disposition: acknowledge
        malformed:
          disposition: dead_letter
          problem:
            model: Problem
    x-workflow: workflow.project.approval_notice
  send_event_project_approved:
    action: send
    channel:
      $ref: '#/channels/event_event_project_approved'
    messages:
    - $ref: '#/components/messages/message_event_project_approved'
    x-emitted-by:
    - application_action.project.approve
  send_event_project_archived:
    action: send
    channel:
      $ref: '#/channels/event_event_project_archived'
    messages:
    - $ref: '#/components/messages/message_event_project_archived'
    x-emitted-by:
    - application_action.project.archive
  send_event_project_created:
    action: send
    channel:
      $ref: '#/channels/event_event_project_created'
    messages:
    - $ref: '#/components/messages/message_event_project_created'
    x-emitted-by:
    - application_action.project.create
  send_event_project_submitted:
    action: send
    channel:
      $ref: '#/channels/event_event_project_submitted'
    messages:
    - $ref: '#/components/messages/message_event_project_submitted'
    x-emitted-by:
    - application_action.project.submit
